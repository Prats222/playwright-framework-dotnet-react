{
  "name": "playwright-framework-dotnet-react",
  "private": true,
  "version": "1.0.0",
  "description": "ASP.NET Core and React practice application with Playwright end-to-end tests",
  "scripts": {
    "install:all": "npm install --prefix ClientApp",
    "build": "npm run build --prefix ClientApp && dotnet build Server/Server.csproj",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.5.0",
    "@playwright/test": "^1.60.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.8.0"
  }
}

